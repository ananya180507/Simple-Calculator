# Keypad Calculator (Flask + HTML + CSS + JS)

A simple keypad-style calculator using Flask backend and JavaScript logic.

## How to Run
1. Install dependencies:
   pip install -r requirements.txt

2. Run the project:
   python app.py

3. Open in browser:
   http://127.0.0.1:5000/
